import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  btn: {
    marginTop: 10,
  },
  cernterView: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
